{
  "name": "carl-actionstudio",
  "version": "1.0.0",
  "description": "Parent package for easy Action Studio development.",
  "main": "main.js",
  "workspaces": [
    "GithubPagesSite",
    "ImmersiveExperience"
  ],
  "scripts": {
    "start": "npm run server --prefix ./GithubPagesSite",
    "build": "npm --prefix ../Targets/babylon run build && npm run build --prefix ./ImmersiveExperience && npm run build --prefix ./GithubPagesSite"
  },
  "author": "syntheticmagus",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
