{
  "name": "carl-actionstudio",
  "version": "1.0.0",
  "description": "Parent package for easy Action Studio development.",
  "main": "main.js",
  "workspaces": [
    "GithubPagesSite",
    "ImmersiveExperience",
    "NativeIntegration"
  ],
  "scripts": {
    "start": "npm run server --prefix ./GithubPagesSite",
    "build": "npm run build --prefix ./NativeIntegration && npm run build --prefix ./ImmersiveExperience && npm run build --prefix ./GithubPagesSite"
  },
  "author": "syntheticmagus",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
